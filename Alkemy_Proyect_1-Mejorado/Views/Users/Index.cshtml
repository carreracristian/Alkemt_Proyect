@model List<Alkemy_Proyect_1.Models.ViewModels.ListUsersViewModel>
@{
    ViewBag.Title = "Admin User";
}

<h2>@ViewBag.Title</h2>

<div class="row">
    <div class="col-md-12">
        <table class="table">
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Lastname</th>
                <th>Dni</th>
            </tr>
            @foreach(var index in Model) { 
            <tr>
                <td>@index.Id</td>
                <td>@index.Name</td>
                <td>@index.Lastname</td>
                <td>@index.Dni</td>
            </tr>
            }
        </table>

    </div>
</div>